apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.frontend.rule.type: PathPrefixStrip
  creationTimestamp: null
  name: metrics-demo
  namespace: dev
spec:
  rules:
  - host: training-user-14.training.weave.works
    http:
      paths:
      - backend:
          serviceName: metrics-demo
          servicePort: http
        path: /mighty-fine
status:
  loadBalancer: {}
